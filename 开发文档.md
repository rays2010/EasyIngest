- 配置
		- AI API key
		- 间隔检查时间
- 输入：一个目录
- 处理
		- 读取输入目录中的存在的视频文件，排除 0 字节，格式可以是 mp4，mkv
		- 读取到的视频文件名通过接口发送给远端 AI，让 AI 对文件名进行清洗，获得最终正确的影片名称和上映时间，然后对文件按照 jellyfin 官方规则，进行重命名
		- 通过 AI 查询该影片的类型，给该影片进行打标签，标签有
			- 电影
			- 电视剧
			- 动画
			- 节目：包含综艺和记录片
- 输出：一个目录
	- 根据影片 上的标签，将影片移动到输出目录下跟标签同名称的文件夹中